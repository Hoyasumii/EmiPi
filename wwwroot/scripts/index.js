// setColors("container");

// window[`setColors`]("container")

// import newPallete from `./colorPallete.json` assert {type: JSON};

// // fetch(`./colorPallete.json`).then((response) => newPallete = response.json());

// console.log(newPallete);